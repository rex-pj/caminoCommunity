@model ArticleCategoryModel
@if (Model.StatusId == ArticleCategoryStatus.Actived)
{
    <span class="badge rounded-pill bg-success">@Model.StatusId</span>
}
else if (Model.StatusId == ArticleCategoryStatus.Inactived)
{
    <span class="badge rounded-pill bg-secondary">@Model.StatusId</span>
}