@model PageListModel<FarmModel>

<table class="table table-bordered">
    <thead>
        <tr class="bg-light">
            <th scope="col">#</th>
            <th scope="col" colspan="2">Farm</th>
            <th scope="col" width="200">Created</th>
            <th scope="col" width="200">Updated</th>
            <th scope="col" width="100"></th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Collections.Any())
        {
            foreach (var farm in Model.Collections)
            {
                farm.SetPageAuthorizationForModel(Model);
                <partial name="_FarmRow" model="farm" />
            }
        }
        else
        {
            <tr>
                <td colspan="6" class="text-center">No data</td>
            </tr>
        }
    </tbody>
</table>

@await Component.InvokeAsync("PageNavigation", Model)