@model FarmTypeModel

<tr>
    <th>@Model.Id</th>
    <th>
        @if (Model.CanUpdate)
        {
            <a asp-action="Update" asp-controller="FarmType" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-dark">
                <i class="fa fa-edit"></i>
            </a>
        }
        <a asp-action="Detail" asp-controller="FarmType" asp-route-id="@Model.Id" class="btn btn-sm btn-outline-info">
            <i class="fa fa-eye"></i>
        </a>
    </th>
    <td>
        <a asp-action="Detail" asp-controller="FarmType" asp-route-id="@Model.Id" class="common-tooltip" data-bs-toggle="tooltip" data-bs-placement="top" title="@Model.Description">
            @Model.Name
        </a>
    </td>
    <td>
        <div>@Model.CreatedDate.ToDateHourMinusFormat()</div>
        <p class="text-muted">by: @Model.CreatedBy</p>
    </td>
    <td>
        <div>@Model.UpdatedDate.ToDateHourMinusFormat()</div>
        <p class="text-muted">by: @Model.UpdatedBy</p>
    </td>
    <td>
        <partial name="Partial/_StatusBadge" />
    </td>
    <td>
        @if (Model.StatusId == FarmTypeStatus.Actived)
        {
            <button type="button" data-id="@Model.Id" data-bs-toggle="modal" data-bs-target="#deactivateModal"
                    class="btn btn-sm btn-outline-dark mb-2 common-tooltip" title="Block this farm type">
                <i class="fa fa-lock"></i>
            </button>
        }
        else if (Model.StatusId == FarmTypeStatus.Inactived)
        {
            <button type="button" data-id="@Model.Id" data-bs-toggle="modal" data-bs-target="#activateModal"
                    class="btn btn-sm btn-outline-success mb-2 common-tooltip" title="Re-activate this farm type">
                <i class="fa fa-unlock"></i>
            </button>
        }

        <button type="button" data-id="@Model.Id" data-bs-toggle="modal" data-bs-target="#permanentlyDeleteModal"
                class="btn btn-sm btn-danger mb-2 common-tooltip" title="Delete this farm type">
            <i class="fa fa-times"></i>
        </button>
    </td>
</tr>